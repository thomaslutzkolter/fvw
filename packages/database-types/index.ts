// Placeholder - wird von Supabase generiert
// Führe 'pnpm generate' aus nach Docker-Start

export type Json =
    | string
    | number
    | boolean
    | null
    | { [key: string]: Json | undefined }
    | Json[]

export interface Database {
    public: {
        Tables: {
            // Wird automatisch generiert
        }
    }
}
